appendFile passa como parametro o nome do arquivo e uma string para armazenar
Caso o arquivo não exista, ele cria o arquivo, caso existe, ele adiciona nele



App.js antigo

console.log('Starting app.js');

const fs = require('fs');
const os = require('os');
const _ = require('lodash')
const notes = require('./notes.js')

// console.log(_.isString(true))
// console.log(_.isString('Leozito trevoso'))

var filteredArray = _.uniq(['Leozito trevoso', 1, 'Leozito trevoso', 1, 2, 3, 4])
console.log(filteredArray)


/* var res = notes.addNote();
console.log(res);
 */

/* var user = os.userInfo();
console.log(user);

fs.appendFileSync('greetings.txt',`Hello ${user.username} ! You are ${notes.age} years old`); 

*/

//notes.soma(9,5)





Podemos usar linhas de comandos para passar argumentos para nossos aplicativos